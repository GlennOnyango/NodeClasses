<%- include('includes/navigation.ejs') %>

    <% if(users.length > 0) {%>
    <ul>
        <% for(let user of users){ %>
        <li><%= user.title %></li>
        <% } %>
    </ul>
    <% } else { %>
    <h1>No user found</h1>
    <% } %>
    
</body>
</html>